name: Keep Render Awake

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 min (Render sleeps after 15 min idle; 5-min ping survives cron delays)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping RaTrade Health
        run: |
          echo "Pinging https://ratrade-tjzd.onrender.com/health"
          curl -fsS --max-time 30 https://ratrade-tjzd.onrender.com/health || echo "health ping failed, trying /"
          curl -fsS --max-time 30 https://ratrade-tjzd.onrender.com/ || echo "root ping failed"
          echo "Ping done at $(date -u)"
